name: Build / Release

on:
  push:
    tags:
      - 'V*'

permissions:
  contents: write

jobs:
  deploy:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Read version
        id: version
        shell: pwsh
        run: |
          $VER = "$env:GITHUB_REF_NAME" -replace '^v',''
          "version=$VER" >> $env:GITHUB_OUTPUT

      - name: Verify version matches
        shell: pwsh
        run: |
          $CODE = Get-Content -Raw 'src/util.py'
          $CODE_VER = [regex]::Match($CODE, 'VERSION = "([^"]+)"').Groups[1].Value
          $TAG_VER = "$env:GITHUB_REF_NAME" -replace '^v',''
          if ($CODE_VER -ne $TAG_VER) {
            Write-Host "版本不一致: tag=$TAG_VER, src/util.py VERSION=$CODE_VER"
            exit 1
          }

      - name: Run tests
        env:
          QT_QPA_PLATFORM: offscreen
        run: python test.py --all

      - name: Create Release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: pwsh
        run: |
          $TAG = "$env:GITHUB_REF_NAME"
          gh release view "$TAG" *> $null
          if ($LASTEXITCODE -ne 0) {
            $PREV = (gh release list --limit 1 --json tagName --jq '.[0].tagName' 2>$null)
            $BODY = if ($PREV) { "**Full Changelog**: https://github.com/$env:GITHUB_REPOSITORY/compare/$PREV...$TAG" } else { "" }
            gh release create "$TAG" --title "$TAG" --notes "$BODY" --latest
          }

  build-windows:
    needs: [deploy]
    strategy:
      matrix:
        include:
          - arch: x64
            os: windows-latest
          - arch: arm64
            os: windows-11-arm
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7

      - name: Setup Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Build
        run: python build.py

      - name: Package zip
        shell: pwsh
        run: |
          Push-Location ../dist
          7z a -mx=9 "$env:GITHUB_WORKSPACE/Windows_${{ matrix.arch }}.zip" O
          Pop-Location

      - name: Upload to release
        env:
          GH_TOKEN: ${{ github.token }}
        shell: pwsh
        run: gh release upload ${{ github.ref_name }} Windows_${{ matrix.arch }}.zip --clobber

  # Linux/macOS 构建处于计划中
  # build-linux:
  #   strategy:
  #     matrix:
  #       include:
  #         - arch: x64
  #           os: ubuntu-22.04
  #         - arch: arm64
  #           os: ubuntu-24.04-arm
  #   runs-on: ${{ matrix.os }}
  #   steps:
  #     - run: echo "Linux build not implemented yet"

  # build-macos:
  #   strategy:
  #     matrix:
  #       include:
  #         - arch: arm64
  #           os: macos-14
  #           openssl_workaround: 'false'
  #         - arch: x64
  #           os: macos-15-intel
  #           openssl_workaround: 'true'
  #   runs-on: ${{ matrix.os }}
  #   steps:
  #     - run: echo "macOS build not implemented yet"